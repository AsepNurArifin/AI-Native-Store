# Template env frontend — salin menjadi `.env` bila perlu override lokal.
# Hanya variabel NUXT_PUBLIC_* (ikut ke bundle browser, JANGAN taruh secret di sini).
NUXT_PUBLIC_API_BASE=http://localhost:8000/api/v1
